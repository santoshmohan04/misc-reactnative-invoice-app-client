# Mobile App Workspace Alias

This folder is a monorepo-friendly placeholder for the Expo mobile app.

Current source of truth remains the repository root app during incremental migration.

Planned target:
- Move Expo app files into this folder in a dedicated migration step.
- Keep app startup commands and backend API contracts unchanged.
