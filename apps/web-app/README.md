# Web App Workspace Alias

This folder is a monorepo-friendly placeholder for the Next.js dashboard app.

Current source of truth remains `web-app/` during incremental migration.

Planned target:
- Move Next.js app files into this folder in a dedicated migration step.
- Keep existing endpoint compatibility and route behavior intact.
